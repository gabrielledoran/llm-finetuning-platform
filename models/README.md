# Models

This directory is reserved for fine-tuning scripts and model checkpoints.

Current work uses pre-trained models accessed via the HuggingFace Hub and API providers (OpenAI, Anthropic). See `notebooks/1_1_setup.ipynb` for the model loading and interface setup.
